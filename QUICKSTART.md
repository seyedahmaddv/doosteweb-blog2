# 🚀 Quick Start Guide

## آسان‌ترین راه برای شروع

### 1️⃣ نصب و راه‌اندازی

```bash
# نصب dependencies
npm install
# یا
pnpm install

# کپی کردن .env.local
cp .env.local.example .env.local

# اجرای سرور توسعه
npm run dev
```

سرور در `http://localhost:3000` اجرا می‌شود

---

### 2️⃣ ورود به سیستم

برو به: **http://localhost:3000/auth/auth1/login**

```
نام کاربری: admin
رمز عبور: admin123
```

✅ بعد از ورود خودکار به `/blog/create` منتقل می‌شوی

---

### 3️⃣ ایجاد پست بلاگ

صفحه برای ایجاد پست جدید:
**http://localhost:3000/blog/create**

#### مراحل:
1. **عنوان**: عنوان پست را وارد کن
2. **دسته‌بندی**: 
   - از لیست موجود انتخاب کن
   - یا دسته جدید اضافه کن
3. **تصویر کاور**:
   - URL تصویر را وارد کن
   - `نمایش پیش‌نمایش` کلیک کن
4. **برچسب‌ها**:
   - برچسب را وارد کن
   - Enter بزن یا دکمه `اضافه کردن` کلیک کن
   - برای حذف روی ✕ کلیک کن
5. **محتوا**:
   - در ویرایشگر محتوا را بنویس
   - از تمام ابزارهای format استفاده کن
6. **ذخیره**:
   - دکمه `ذخیره پست` کلیک کن

---

### 4️⃣ نتایج

پست ذخیره شده:
- ✅ در دیتابیس
- ✅ با تمام جزئیات
- ✅ شامل تصویر، دسته، برچسب‌ها

برای دیدن تمام پست‌ها:
**http://localhost:3000/blog**

---

## 🔑 کلیدی ویژگی‌ها

| ویژگی | توضیح |
|------|-------|
| 🖼️ تصویر کاور | نمایش پیش‌نمایش + ذخیره |
| 📂 دسته سفارشی | اضافه کردن دسته جدید |
| 🏷️ برچسب‌ها | چند برچسب فی پست |
| 🔐 احراز هویت | فقط ادمین |
| 🛡️ محافظت | صفحات محافظت‌شده |

---

## 📊 دیتابیس

### اگر دیتابیس جدید است:

```bash
# ایجاد schema
psql "$DATABASE_URL" -f src/db/schema.sql

# اضافه کردن tags
psql "$DATABASE_URL" -f src/db/migration-002.sql
```

---

## 🔧 تنظیمات

### تغییر رمز عبور ادمین:

فایل: `src/app/api/auth/login/route.ts`

```typescript
const ADMIN_USER = "admin";  // تغییر این
const ADMIN_PASSWORD = "admin123";  // یا این
```

---

## ❓ مشکلات رایج

### مشکل: "Cannot find database"
```bash
# بررسی DATABASE_URL در .env.local
echo $DATABASE_URL

# یا اجرای schema دوباره
psql "$DATABASE_URL" -f src/db/schema.sql
```

### مشکل: "Unauthorized" در /blog/create
```bash
# لاگین دوباره کن
# برو به http://localhost:3000/auth/auth1/login
```

### مشکل: تصویر نمایش نمی‌دهد
```
# از URL عمومی (public URL) استفاده کن
# مثال: https://example.com/image.jpg
```

---

## 📚 فایل‌های مرجع

- 📖 [IMPLEMENTATION_SUMMARY_FA.md](./IMPLEMENTATION_SUMMARY_FA.md) - تفاصیل کامل
- 📖 [BLOG_FEATURES.md](./BLOG_FEATURES.md) - ویژگی‌های بلاگ
- 📖 [src/db/README_NEW.md](./src/db/README_NEW.md) - دیتابیس

---

## 💡 نکات

✨ **تمام ویژگی‌ها در `blog-editor.tsx` هستند**

✨ **احراز هویت با JWT Tokens**

✨ **محافظت با Middleware**

---

## ✅ شطرنج‌بازی

- [ ] وارد شدن به سیستم
- [ ] رفتن به صفحه ایجاد پست
- [ ] افزودن عنوان
- [ ] انتخاب دسته‌بندی
- [ ] اضافه کردن تصویر کاور
- [ ] نمایش پیش‌نمایش
- [ ] افزودن برچسب‌ها
- [ ] نوشتن محتوا
- [ ] ذخیره پست
- [ ] دیدن پست در لیست

---

## 🎉 بزن رو شروع کن!

```bash
npm run dev
# ↓
# http://localhost:3000/auth/auth1/login
# ↓
# admin / admin123
# ↓
# http://localhost:3000/blog/create
```

---

سوالی دارید؟ فایل‌های مرجع را بخوانید! 📖
