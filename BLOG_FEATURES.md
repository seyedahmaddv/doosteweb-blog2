# 📋 راهنمای تغییرات بلاگ

## ✅ ویژگی‌های پیاده‌سازی‌شده

### 1️⃣ **نمایش تصویر شاخص پست (Cover Image)**
- ✅ بارگذاری URL تصویر
- ✅ پیش‌نمایش تصویر قبل از ذخیره
- ✅ ذخیره خودکار در پایگاه داده

### 2️⃣ **دسته‌بندی‌های سفارشی (Custom Categories)**
- ✅ دسته‌بندی‌های پیش‌فرض
- ✅ امکان افزودن دسته‌بندی جدید از طریق UI
- ✅ ذخیره در حافظه

### 3️⃣ **برچسب‌ها (Tags)**
- ✅ افزودن چندین برچسب
- ✅ حذف برچسب‌ها
- ✅ ذخیره به صورت JSON در دیتابیس
- ✅ کمبوکس برای دیدن برچسب‌های اضافه‌شده

### 4️⃣ **احراز هویت و محافظت**
- ✅ لاگین ادمین فقط
- ✅ JWT Token Authentication
- ✅ Cookie-based session management
- ✅ Middleware برای محافظت از صفحات
- ✅ Logout API

---

## 🔐 احراز هویت

### مشخصات ورود:
```
Username: admin
Password: admin123
```

### فلوی احراز هویت:

1. **صفحه ورود** → `/auth/auth1/login`
   - وارد کردن نام کاربری و رمز عبور
   - دریافت JWT Token
   - تنظیم Cookie

2. **بررسی احراز هویت** → `/api/auth/me`
   - بررسی کردن validity توکن
   - بازگرداندن اطلاعات کاربر

3. **خروج** → `/api/auth/logout`
   - حذف Cookie
   - بازگردان به صفحه خانه

---

## 📁 فایل‌های به‌روز‌شده

### API Routes:
- `src/app/api/auth/login/route.ts` - JWT Token generation
- `src/app/api/auth/me/route.ts` - کاربر جاری
- `src/app/api/auth/logout/route.ts` - خروج
- `src/app/api/blog/posts/route.ts` - ذخیره tags و cover image

### Components:
- `src/components/blog-editor.tsx` - ویرایشگر بلاگ جدید
  - پیش‌نمایش تصویر
  - مدیریت برچسب‌ها
  - افزودن دسته‌بندی جدید
  - بررسی احراز هویت ادمین

### Middleware:
- `middleware.ts` - محافظت از صفحات ادمین

### Database:
- `src/db/schema.sql` - افزودن ستون `tags` به جدول posts

### Types:
- `src/types/blog.ts` - اضافه کردن Category و Tag types

---

## 🚀 نحوه استفاده

### 1. ورود به سیستم
```bash
# برو به صفحه ورود
/auth/auth1/login

# وارد کن:
Username: admin
Password: admin123
```

### 2. ایجاد پست جدید
```bash
# برو به صفحه ایجاد
/blog/create

# پس از ورود می‌توانی:
- عنوان پست را وارد کنی
- دسته‌بندی را انتخاب کنی
- دسته‌بندی جدید اضافه کنی
- تصویر کاور را بارگذاری کنی
- پیش‌نمایش تصویر را ببینی
- برچسب‌های متعدد اضافه کنی
- محتوای پست را بنویسی
- پست را ذخیره کنی
```

### 3. مثال - افزودن برچسب:
```
برچسب جدید: React
برچسب جدید: Next.js
برچسب جدید: بلاگ
```

### 4. مثال - افزودن دسته‌بندی:
```
دسته جدید: فرانت‌اند
دسته جدید: باک‌اند
دسته جدید: دیتابیس
```

---

## 💾 ذخیره‌سازی

### در دیتابیس:
```sql
posts {
  id: number
  title: string
  content: html
  cover_img: string (URL)
  category: string
  tags: JSON array ["tag1", "tag2", ...]
  author_id: number
  created_at: timestamp
}
```

### جدول schema جدید:
```sql
ALTER TABLE posts ADD COLUMN tags JSONB;
```

---

## 🔒 امنیت

- JWT Tokens امضا‌شده
- HttpOnly Cookies
- Middleware validation
- Role-based access (admin فقط)
- Automatic redirect اگر لاگین نشده

---

## ⚠️ نکات مهم

1. **تغییر رمز عبور**: فایل `src/app/api/auth/login/route.ts` را ویرایش کنید
2. **JWT Secret**: متغیر محیطی `JWT_SECRET` را تنظیم کنید
3. **تصویرها**: از URLs مطمئن استفاده کنید
4. **برچسب‌ها**: فارسی یا انگلیسی قابل قبول است

---

## 🧪 تست کردن

### صفحات محافظت‌شده:
```
❌ /blog/create (بدون لاگین)
✅ /blog/create (با لاگین ادمین)
```

### API endpoints:
```bash
# بررسی لاگین
curl -X GET http://localhost:3000/api/auth/me

# ورود
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# خروج
curl -X POST http://localhost:3000/api/auth/logout
```

---

## 📝 لیست ویژگی‌ها

- [x] نمایش تصویر شاخص پست
- [x] دسته‌بندی‌های سفارشی
- [x] سیستم برچسب‌ها
- [x] احراز هویت JWT
- [x] Cookie-based sessions
- [x] صفحات محافظت‌شده
- [x] Middleware validation
- [x] پیش‌نمایش تصویر
- [x] بررسی ادمین

---

## ✨ بعدی (Optional)

- [ ] دیتابیس برای دسته‌بندی‌ها
- [ ] دیتابیس برای برچسب‌ها
- [ ] صفحه مدیریت پست‌ها
- [ ] صفحه مدیریت نظرات
- [ ] صفحه آمار
- [ ] تصویر-آپ‌لود (بدون URL)
